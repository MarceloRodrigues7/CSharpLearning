<Window x:Class="Gestor.UI.FrmCategorias"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Gestor.UI"
        mc:Ignorable="d"
        Title="Categorias" Height="200" Width="400">
    <Grid>
        <Grid.ColumnDefinitions>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Button x:Name="btnAdicionar" Grid.Row="0" Grid.Column="0" Width="100" HorizontalAlignment="Left" 
                Content="Adicionar" Margin="5,5,0,5" Click="btnAdicionar_Click"/>
        <Button x:Name="btnAtualizar" Grid.Row="0" Grid.Column="0" Width="100" HorizontalAlignment="Right" 
                Content="Atualizar" Margin="0,5,5,5" Click="btnAtualizar_Click"/>
        <DataGrid x:Name="dtCategorias" Grid.Row="2" Grid.Column="0" IsReadOnly="True"/>
    </Grid>
</Window>
